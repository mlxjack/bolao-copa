function renderPlayerPredictions(player){const body=document.querySelector('#playerPredictionsBody');if(!player.predictions.length){body.innerHTML='<tr><td colspan="4" class="empty">Nenhum palpite cadastrado.</td></tr>';return}body.innerHTML=player.predictions.map(entry=>{const game=entry.game,result=game.homeScore===null||game.awayScore===null?'Aguardando':`${game.homeScore} x ${game.awayScore}`,prediction=entry.homeScore===null||entry.awayScore===null?'Nao preenchido':`${entry.homeScore} x ${entry.awayScore}`;return `<tr class="${entry.pointsNow>0?'prediction-scored':''}"><td data-label="Jogo"><strong class="player-game-teams">${teamsLine(game)}</strong><div class="match-meta">${gameMeta(game)}</div></td><td data-label="Meu palpite"><span class="prediction-score">${prediction}</span></td><td data-label="Resultado"><span class="result-score ${game.status}">${result}</span></td><td data-label="Pontos"><strong class="live-points ${entry.pointsNow>0?'scoring':''}">${entry.pointsNow}</strong></td></tr>`}).join('')}
