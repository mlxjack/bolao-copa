function livePositionDetails(predictions){const currentRanks=sharedRankMap(state.standings),predictionMap=new Map(predictions.map(p=>[p.playerId,p.pointsNow])),beforeLive=state.standings.map(player=>{const livePoints=predictionMap.get(player.id)||0;return{...player,points:player.points-livePoints,exact:player.exact-(livePoints===5?1:0),outcome:player.outcome-(livePoints===3?1:0)}}).sort((a,b)=>b.points-a.points||b.exact-a.exact||a.name.localeCompare(b.name,'pt-BR')),previousRanks=sharedRankMap(beforeLive);return new Map(state.standings.map(player=>{const before=previousRanks.get(player.id),current=currentRanks.get(player.id);return[player.id,{before,current,change:before-current,direction:current<before?'up':current>before?'down':'same'}]}))}function movementLabel(d){if(!d)return'';if(d.direction==='up')return `<span class="live-movement up"><strong>${d.before}º → ${d.current}º</strong><small>Subindo para ${d.current}º</small></span>`;if(d.direction==='down')return `<span class="live-movement down"><strong>${d.before}º → ${d.current}º</strong><small>Caindo para ${d.current}º</small></span>`;return `<span class="live-movement same"><strong>${d.current}º</strong><small>Permanece em ${d.current}º</small></span>`}async function renderLiveGame(){const select=document.querySelector('#liveGameSelect'),gameId=select.value||state.games[0]?.id,scoreboard=document.querySelector('#liveScoreboard'),body=document.querySelector('#livePredictionsBody');if(!gameId){scoreboard.innerHTML='<div class="empty">Nenhum jogo cadastrado.</div>';body.innerHTML='';return}const data=await api(`/api/games/${gameId}/predictions`),game=data.game,score=game.homeScore===null||game.awayScore===null?'- x -':`${game.homeScore} x ${game.awayScore}`,homeLeading=Number(game.homeScore)>Number(game.awayScore),awayLeading=Number(game.awayScore)>Number(game.homeScore);scoreboard.innerHTML=`<div class="score-team ${homeLeading?'winning':''}">${teamLabel(game.homeTeam,'large')}</div><div class="score-center"><span class="score-stage">${escapeHtml(game.group?`Grupo ${game.group}`:game.stage||'')}</span><div class="score-number">${score}</div><span class="badge ${game.status}">${labels[game.status]}</span></div><div class="score-team ${awayLeading?'winning':''}">${teamLabel(game.awayTeam,'large')}</div>`;updateSelectedGameLabel();renderGamesDrawer();const summary=document.querySelector('#liveSummary');if(data.predictions.length===0){summary.innerHTML='';body.innerHTML='<tr><td colspan="4" class="empty">Ainda nao ha palpites para esse jogo.</td></tr>';return}const predictions=[...data.predictions].sort((a,b)=>b.pointsNow-a.pointsNow||a.playerName.localeCompare(b.playerName,'pt-BR')),positions=game.status==='live'?livePositionDetails(predictions):new Map(),movingUp=[...positions.values()].filter(d=>d.direction==='up').length,movingDown=[...positions.values()].filter(d=>d.direction==='down').length;summary.classList.toggle('upcoming',game.status==='upcoming');summary.classList.toggle('movement-summary',game.status!=='upcoming');summary.innerHTML=game.status==='upcoming'?`<div><strong>${predictions.length}</strong><span>palpites já disponíveis para esta partida</span></div>`:`<div class="summary-up"><strong>↑ ${movingUp}</strong><span>subindo na tabela</span></div><div class="summary-down"><strong>↓ ${movingDown}</strong><span>caindo na tabela</span></div>`;body.innerHTML=predictions.map(prediction=>{const detail=positions.get(prediction.playerId);return `<tr class="live-player-card"><td class="live-player-info" data-label="Participante" colspan="2"><span class="live-player-name"><strong>${escapeHtml(prediction.playerName)}</strong></span>${game.status==='live'?movementLabel(detail):''}</td><td class="live-player-prediction" data-label="Palpite"><span class="prediction-score">${prediction.homeScore} x ${prediction.awayScore}</span></td><td class="live-player-points" data-label="Pontos agora"><strong class="live-points ${prediction.pointsNow>0?'scoring':''}">${prediction.pointsNow}</strong></td></tr>`}).join('')}
